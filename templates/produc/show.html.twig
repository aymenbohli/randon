{% extends 'base.html.twig' %}

{% block title %}Produc{% endblock %}

{% block body %}
    <h1>Produc</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ produc.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ produc.name }}</td>
            </tr>
            <tr>
                <th>Descp</th>
                <td>{{ produc.descp }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('produc_index') }}">back to list</a>

    <a href="{{ path('produc_edit', {'id': produc.id}) }}">edit</a>

    {{ include('produc/_delete_form.html.twig') }}
{% endblock %}
